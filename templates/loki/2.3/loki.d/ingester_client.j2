{% if loki_config_ingester_client is defined and loki_config_ingester_client | count > 0 %}
ingester_client:
  {{ loki_config_ingester_client | to_nice_yaml(indent=2) | indent(2, False) }}
{% endif %}
